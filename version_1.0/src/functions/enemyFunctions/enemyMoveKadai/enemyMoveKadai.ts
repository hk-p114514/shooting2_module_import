import { Enemy } from '../../../classes/Enemy';
import { enemySpriteStart } from '../../../init/spriteInit';

const enemyMoveKadai = (
	enemy: Enemy,
	sp: number = enemySpriteStart.kadai,
) => {};

export { enemyMoveKadai };
